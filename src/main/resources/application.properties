spring.application.name=cinema-managing-system

# MongoDB Atlas Configuration
spring.data.mongodb.uri=mongodb+srv://viduranec21018_db_user:1PT66Tbaga2DGbxL@cluster0.hrph9cw.mongodb.net/?retryWrites=true&w=majority&appName=cinema-managing-system&maxPoolSize=50&minPoolSize=10&maxIdleTimeMS=300000&connectTimeoutMS=30000&socketTimeoutMS=60000
spring.data.mongodb.database=cinema_db

# Server Configuration
server.port=8081

# MongoDB Connection Settings
spring.data.mongodb.auto-index-creation=true

# MongoDB Connection Pool Settings
spring.data.mongodb.connection-timeout=30000
spring.data.mongodb.socket-timeout=60000
spring.data.mongodb.server-selection-timeout=30000

# Graceful Shutdown Settings
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s

# Keep application alive
spring.main.keep-alive=true

# JWT Configuration
app.jwt.secret=mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLongForHS256Algorithm
app.jwt.expiration=86400000

# Mail Configuration (Update with your SMTP settings)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
